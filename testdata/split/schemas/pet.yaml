# Pet and NewPet schemas.
# Referenced from openapi.yaml via $ref.

Pet:
  type: object
  required: [id, name]
  properties:
    id:
      type: integer
      format: int64
      readOnly: true
    name:
      type: string
      x-validations: ["Size >=1"]
    tag:
      type: string
      nullable: true
    secret:
      type: string
      x-sensitive: true

NewPet:
  type: object
  required: [name]
  properties:
    name:
      type: string
      x-validations: ["Size >=1"]
    tag:
      type: string
      nullable: true
    secret:
      type: string
      x-sensitive: true

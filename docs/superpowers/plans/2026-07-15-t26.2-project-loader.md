# T26.2 — ProjectLoader Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Implement `ProjectLoader.Load(input, flagsLoader, importPrefix, output)` that discovers services, parses each spec, builds `ProjectSet` with populated `Model`/`Paths`, and returns a minimal `SchemaIndex` for T26.3 to enrich.

**Architecture:** `ProjectLoader` walks the input directory for service folders (`<input>/<folder>/src/openapi/openapi.yaml`), parses each spec via existing `parser.ParseFile`, loads features via `GenerationFlagsLoader`, and constructs `Project` via the T26.1 factories (`CreateModel`/`CreatePaths`). Schemas are transferred from `doc.Schemas` to `Model.schemas`; operations are routed to `Paths.AddMethod` via the existing `serviceName` helper. `markExternalRefs` and per-schema `SchemaIndex` population are deferred to T26.3 (source-marking).

**Tech Stack:** Go 1.x, `github.com/pb33f/libopenapi`, `github.com/stretchr/testify`, existing `internal/parser` + `internal/fs` packages.

**Branch:** `feat/project-loader` (off `feat/project-set-types`).

---

## Scope boundaries (from rev2 spec + user choice A)

**In scope (T26.2):**
- `walkServices(input)` → `[]serviceDescriptor` (filesystem discovery)
- `ProjectLoader.Load(...)` pipeline (steps 1-6, 8-9 of spec §T26.2)
- Schema transfer `doc.Schemas` → `project.Model.schemas` + `Model.Index()`
- Operation transfer `doc.Operations` → `project.Paths.AddMethod(serviceName(op), op)`
- Minimal `SchemaIndex` (struct returned, but `Schemas` map empty — populated in T26.3)
- Common project handling (special `common` folder, sets `Model.Prefix`)

**Out of scope (T26.3):**
- `markExternalRefs(doc, ...)` — NOT called by T26.2
- `Schema.SourceFile`/`ExternalRef`/`OwnerProject` fields — NOT added
- Per-schema `SchemaIndex` population — T26.2 returns `SchemaIndex` with empty `Schemas` map
- `SchemaIndex.LookupByFile` returning real data — still returns `false` (no entries)

**Consequence:** T26.2's `SchemaIndex` is structurally present but functionally empty. T26.3 fills it after adding `SourceFile`. Generator (T26.4-T26.5) does not consume the index until T26.3 lands.

---

## File Structure

- **Create:** `internal/parser/project_loader.go` — `ProjectLoader`, `NewProjectLoader`, `Load`, `walkServices`, `serviceDescriptor`
- **Create:** `internal/parser/project_loader_test.go` — unit tests (white-box, `package parser`)
- **Create:** `internal/parser/testdata/multiservice/` — fixture tree:
  - `common/src/openapi/openapi.yaml` (2 schemas, 0 operations)
  - `userBackend/src/openapi/openapi.yaml` (1 schema, 2 operations tagged `UserBackend`)
  - `authBackend/src/openapi/openapi.yaml` (1 schema, 1 operation tagged `AuthBackend`)
  - `generation_flags.yaml` (global flags config, all defaults)
- **No changes** to existing `project_set.go`, `model.go`, `paths_wrapper.go`, `paths.go`, `schema_index.go` — their APIs are consumed as-is.

---

## Task 1: `serviceDescriptor` struct and fixture scaffold

**Files:**
- Create: `internal/parser/project_loader.go`
- Create: `internal/parser/testdata/multiservice/common/src/openapi/openapi.yaml`
- Create: `internal/parser/testdata/multiservice/userBackend/src/openapi/openapi.yaml`
- Create: `internal/parser/testdata/multiservice/authBackend/src/openapi/openapi.yaml`

- [ ] **Step 1: Write the failing test**

Create `internal/parser/project_loader_test.go`:

```go
package parser

import (
	"testing"

	"github.com/stretchr/testify/assert"
)

func TestServiceDescriptor_Fields(t *testing.T) {
	d := serviceDescriptor{
		Folder:    "userBackend",
		SpecPath:  "/input/userBackend/src/openapi/openapi.yaml",
		FlagsPath: "/input/userBackend/generation_flags.yaml",
	}
	assert.Equal(t, "userBackend", d.Folder)
	assert.Equal(t, "/input/userBackend/src/openapi/openapi.yaml", d.SpecPath)
	assert.Equal(t, "/input/userBackend/generation_flags.yaml", d.FlagsPath)
}
```

Create minimal `internal/parser/project_loader.go`:

```go
package parser

// serviceDescriptor описывает один обнаруженный сервис во входном каталоге.
type serviceDescriptor struct {
	Folder    string // относительный путь от input ("userBackend", "common")
	SpecPath  string // абсолютный путь к src/openapi/openapi.yaml
	FlagsPath string // абсолютный путь к generation_flags.yaml (может быть пустым)
}
```

- [ ] **Step 2: Run test to verify it passes**

Run: `go test ./internal/parser/ -run TestServiceDescriptor_Fields -v`
Expected: PASS (struct field assignment).

- [ ] **Step 3: Create test fixtures**

`internal/parser/testdata/multiservice/common/src/openapi/openapi.yaml`:

```yaml
openapi: 3.0.3
info:
  title: Common
  version: 1.0.0
paths: {}
components:
  schemas:
    User:
      type: object
      properties:
        id:
          type: string
        name:
          type: string
    Profile:
      type: object
      properties:
        bio:
          type: string
```

`internal/parser/testdata/multiservice/userBackend/src/openapi/openapi.yaml`:

```yaml
openapi: 3.0.3
info:
  title: UserBackend
  version: 1.0.0
paths:
  /users:
    get:
      operationId: ListUsers
      tags: [UserBackend]
      responses:
        '200':
          description: ok
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserList'
    post:
      operationId: CreateUser
      tags: [UserBackend]
      responses:
        '201':
          description: created
components:
  schemas:
    UserList:
      type: array
      items:
        type: string
```

`internal/parser/testdata/multiservice/authBackend/src/openapi/openapi.yaml`:

```yaml
openapi: 3.0.3
info:
  title: AuthBackend
  version: 1.0.0
paths:
  /login:
    post:
      operationId: Login
      tags: [AuthBackend]
      responses:
        '200':
          description: ok
components:
  schemas:
    LoginRequest:
      type: object
      properties:
        token:
          type: string
```

- [ ] **Step 4: Commit**

```bash
git add internal/parser/project_loader.go internal/parser/project_loader_test.go internal/parser/testdata/multiservice/
git commit -m "feat(parser): add serviceDescriptor struct and multiservice test fixtures"
```

---

## Task 2: `walkServices` — discover service directories

**Files:**
- Modify: `internal/parser/project_loader.go`
- Modify: `internal/parser/project_loader_test.go`

- [ ] **Step 1: Write the failing test**

Append to `project_loader_test.go`:

```go
func TestWalkServices_DiscoversAllServices(t *testing.T) {
	descs, err := walkServices("testdata/multiservice")
	assert.NoError(t, err)
	assert.Len(t, descs, 3)

	byFolder := map[string]serviceDescriptor{}
	for _, d := range descs {
		byFolder[d.Folder] = d
	}

	assert.Contains(t, byFolder, "common")
	assert.Contains(t, byFolder, "userBackend")
	assert.Contains(t, byFolder, "authBackend")

	// SpecPath — абсолютный путь к openapi.yaml внутри сервиса
	assert.True(t, strings.HasSuffix(byFolder["userBackend"].SpecPath, "userBackend/src/openapi/openapi.yaml"))
}

func TestWalkServices_NoServicesDir(t *testing.T) {
	_, err := walkServices("testdata/nonexistent")
	assert.Error(t, err)
	assert.Contains(t, err.Error(), "stat")
}
```

Add `"strings"` to imports.

- [ ] **Step 2: Run test to verify it fails**

Run: `go test ./internal/parser/ -run TestWalkServices -v`
Expected: FAIL — `walkServices undefined`.

- [ ] **Step 3: Implement `walkServices`**

Append to `project_loader.go`:

```go
import (
	"os"
	"path/filepath"
	"sort"
)

// walkServices обходит input-каталог и находит все сервисы по наличию
// src/openapi/openapi.yaml. Возвращает дескрипторы, отсортированные по Folder
// для детерминированной обработки. Каталог "common" (если есть) попадает в
// общий список — ProjectLoader.Load выделит его отдельно.
func walkServices(input string) ([]serviceDescriptor, error) {
	entries, err := os.ReadDir(input)
	if err != nil {
		return nil, fmt.Errorf("read input dir %q: %w", input, err)
	}

	var out []serviceDescriptor
	for _, entry := range entries {
		if !entry.IsDir() {
			continue
		}
		folder := entry.Name()
		specPath := filepath.Join(input, folder, "src", "openapi", "openapi.yaml")
		if _, err := os.Stat(specPath); err != nil {
			continue // не сервис — пропускаем
		}
		desc := serviceDescriptor{
			Folder:   folder,
			SpecPath: specPath,
		}
		flagsPath := filepath.Join(input, folder, "generation_flags.yaml")
		if _, err := os.Stat(flagsPath); err == nil {
			desc.FlagsPath = flagsPath
		}
		out = append(out, desc)
	}

	sort.Slice(out, func(i, j int) bool {
		return out[i].Folder < out[j].Folder
	})
	return out, nil
}
```

Add `"fmt"` to imports.

- [ ] **Step 4: Run test to verify it passes**

Run: `go test ./internal/parser/ -run TestWalkServices -v`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add internal/parser/project_loader.go internal/parser/project_loader_test.go
git commit -m "feat(parser): add walkServices for service directory discovery"
```

---

## Task 3: `ProjectLoader` struct and constructor

**Files:**
- Modify: `internal/parser/project_loader.go`
- Modify: `internal/parser/project_loader_test.go`

- [ ] **Step 1: Write the failing test**

Append:

```go
func TestNewProjectLoader(t *testing.T) {
	pl := NewProjectLoader()
	assert.NotNil(t, pl)
}
```

- [ ] **Step 2: Run to verify it fails**

Run: `go test ./internal/parser/ -run TestNewProjectLoader -v`
Expected: FAIL — `NewProjectLoader undefined`.

- [ ] **Step 3: Implement**

Append to `project_loader.go`:

```go
// ProjectLoader собирает ProjectSet из входного каталога: обнаруживает
// сервисы, парсит спеки, загружает generation flags, переносит схемы и
// операции в Model/Paths.
type ProjectLoader struct{}

// NewProjectLoader создаёт ProjectLoader. Stateless — все параметры
// передаются в Load.
func NewProjectLoader() *ProjectLoader {
	return &ProjectLoader{}
}
```

- [ ] **Step 4: Run to verify it passes**

Run: `go test ./internal/parser/ -run TestNewProjectLoader -v`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add internal/parser/project_loader.go internal/parser/project_loader_test.go
git commit -m "feat(parser): add ProjectLoader struct and constructor"
```

---

## Task 4: `Load` — happy path, single service (no common)

**Files:**
- Modify: `internal/parser/project_loader.go`
- Modify: `internal/parser/project_loader_test.go`

- [ ] **Step 1: Write the failing test**

Append:

```go
func TestProjectLoader_Load_SingleService(t *testing.T) {
	pl := NewProjectLoader()
	ps, si, err := pl.Load(
		"testdata/multiservice",
		nil, // no flagsLoader — defaults
		"nschugorev/oapigenerator/go",
		"/output",
	)
	assert.NoError(t, err)
	assert.NotNil(t, ps)
	assert.NotNil(t, si)

	// Один проект: userBackend + authBackend + common = 3 (common выделяется)
	assert.Len(t, ps.Projects, 2)
	assert.NotNil(t, ps.Common, "common project must be detected and separated")
}
```

- [ ] **Step 2: Run to verify it fails**

Run: `go test ./internal/parser/ -run TestProjectLoader_Load_SingleService -v`
Expected: FAIL — `Load undefined`.

- [ ] **Step 3: Implement `Load` skeleton**

Append to `project_loader.go`:

```go
import (
	"nschugorev/oapigenerator/internal/codegen/gogen"
	"nschugorev/oapigenerator/internal/fs"
	"path/filepath"
)

// Load собирает ProjectSet из input-каталога.
//
// input — абсолютный или относительный путь к корню проекта (содержит
//   подпапки сервисов: common/, userBackend/, ...).
// flagsLoader — загрузчик generation flags (может быть nil — используются
//   дефолтные значения для всех проектов).
// importPrefix — Go import path корня (например "nschugorev/oapigenerator/go").
// output — абсолютный путь к output-каталогу.
//
// Возвращает ProjectSet (с заполненными Projects, Common, ByName) и
// SchemaIndex (пустой — заполняется в T26.3 после source-marking).
func (pl *ProjectLoader) Load(
	input string,
	flagsLoader *GenerationFlagsLoader,
	importPrefix, output string,
) (*ProjectSet, *SchemaIndex, error) {
	descs, err := walkServices(input)
	if err != nil {
		return nil, nil, fmt.Errorf("walk services: %w", err)
	}

	ps := &ProjectSet{
		ByName: map[string]*Project{},
	}
	si := &SchemaIndex{Schemas: map[string]*SchemaEntry{}}

	for _, desc := range descs {
		project, err := pl.loadProject(desc, flagsLoader, importPrefix, output)
		if err != nil {
			return nil, nil, fmt.Errorf("load project %q: %w", desc.Folder, err)
		}

		if desc.Folder == "common" {
			ps.Common = project
			project.Model.Prefix = "common"
		}
		ps.Projects = append(ps.Projects, project)
		ps.ByName[project.Folder] = project
	}

	return ps, si, nil
}

// loadProject парсит один сервис и строит Project с Model и Paths.
func (pl *ProjectLoader) loadProject(
	desc serviceDescriptor,
	flagsLoader *GenerationFlagsLoader,
	importPrefix, output string,
) (*Project, error) {
	fsys := fs.NewRealFS()
	doc, err := ParseFile(fsys, desc.SpecPath)
	if err != nil {
		return nil, fmt.Errorf("parse spec: %w", err)
	}

	features, err := pl.resolveFeatures(flagsLoader, desc.FlagsPath)
	if err != nil {
		return nil, fmt.Errorf("resolve features: %w", err)
	}

	folder := desc.Folder
	project := &Project{
		Folder:       folder,
		SpecPath:     desc.SpecPath,
		FlagsPath:    desc.FlagsPath,
		Features:     features,
		OutputDir:    filepath.Join(output, folder),
		ImportPrefix: filepath.Join(importPrefix, folder),
	}

	modelImp := gogen.Import{
		Path:    project.ImportPrefix + "/model",
		Alias:   "model",
		Package: "",
	}
	project.CreateModel(modelImp)
	project.CreatePaths(project.ImportPrefix)

	// Перенос схем
	project.Model.schemas = doc.Schemas
	project.Model.Index()

	// Перенос операций
	for _, op := range doc.Operations {
		svcName, err := serviceName(op.Method, op.Path, op.Tags)
		if err != nil {
			return nil, fmt.Errorf("service name for %s %s: %w", op.Method, op.Path, err)
		}
		project.Paths.AddMethod(svcName, op)
	}

	return project, nil
}

// resolveFeatures возвращает ProjectFeatures для сервиса. Если flagsLoader
// nil или FlagsPath пустой — возвращает дефолтные значения.
func (pl *ProjectLoader) resolveFeatures(
	flagsLoader *GenerationFlagsLoader,
	flagsPath string,
) (ProjectFeatures, error) {
	if flagsLoader == nil || flagsPath == "" {
		return ProjectFeatures{}, nil
	}
	return flagsLoader.GetProjectFeatures(flagsPath)
}
```

Note: `filepath` is already imported from Task 2; remove the duplicate from the new import block. Merge imports cleanly.

- [ ] **Step 4: Run to verify it passes**

Run: `go test ./internal/parser/ -run TestProjectLoader_Load_SingleService -v`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add internal/parser/project_loader.go internal/parser/project_loader_test.go
git commit -m "feat(parser): implement ProjectLoader.Load with single-service path"
```

---

## Task 5: `Load` — verify Model schemas transfer and Index

**Files:**
- Modify: `internal/parser/project_loader_test.go`

- [ ] **Step 1: Write the failing test**

Append:

```go
func TestProjectLoader_Load_ModelSchemasTransferred(t *testing.T) {
	pl := NewProjectLoader()
	ps, _, err := pl.Load("testdata/multiservice", nil,
		"nschugorev/oapigenerator/go", "/output")
	assert.NoError(t, err)

	common := ps.Common
	assert.NotNil(t, common.Model)
	assert.Len(t, common.Model.Schemas(), 2, "common must have User + Profile")

	// Index построен — Lookup работает
	user, ok := common.Model.Lookup("User")
	assert.True(t, ok)
	assert.Equal(t, "User", user.Name)

	profile, ok := common.Model.Lookup("Profile")
	assert.True(t, ok)
	assert.Equal(t, "Profile", profile.Name)

	_, ok = common.Model.Lookup("Nonexistent")
	assert.False(t, ok)
}
```

- [ ] **Step 2: Run to verify it passes (already implemented in Task 4)**

Run: `go test ./internal/parser/ -run TestProjectLoader_Load_ModelSchemasTransferred -v`
Expected: PASS — verifies the schema transfer + Index() call from Task 4.

- [ ] **Step 3: Commit**

```bash
git add internal/parser/project_loader_test.go
git commit -m "test(parser): verify Model schemas transfer and Index in ProjectLoader"
```

---

## Task 6: `Load` — verify Paths AddMethod via serviceName

**Files:**
- Modify: `internal/parser/project_loader_test.go`

- [ ] **Step 1: Write the failing test**

Append:

```go
func TestProjectLoader_Load_PathsServicesGroupedByTag(t *testing.T) {
	pl := NewProjectLoader()
	ps, _, err := pl.Load("testdata/multiservice", nil,
		"nschugorev/oapigenerator/go", "/output")
	assert.NoError(t, err)

	userBackend := ps.ByName["userBackend"]
	assert.NotNil(t, userBackend.Paths)
	assert.Len(t, userBackend.Paths.Services, 1, "userBackend has one tag → one service")
	assert.Equal(t, "UserBackend", userBackend.Paths.Services[0].Name)
	assert.Len(t, userBackend.Paths.Services[0].Methods, 2, "ListUsers + CreateUser")

	authBackend := ps.ByName["authBackend"]
	assert.Len(t, authBackend.Paths.Services, 1)
	assert.Equal(t, "AuthBackend", authBackend.Paths.Services[0].Name)
	assert.Len(t, authBackend.Paths.Services[0].Methods, 1, "Login")
}

func TestProjectLoader_Load_CommonHasNoServices(t *testing.T) {
	pl := NewProjectLoader()
	ps, _, err := pl.Load("testdata/multiservice", nil,
		"nschugorev/oapigenerator/go", "/output")
	assert.NoError(t, err)

	// common/spec имеет paths: {} — нет операций → нет сервисов
	assert.Empty(t, ps.Common.Paths.Services)
}
```

- [ ] **Step 2: Run to verify it passes**

Run: `go test ./internal/parser/ -run "TestProjectLoader_Load_Paths" -v`
Expected: PASS — verifies operation transfer via `AddMethod(serviceName(...))`.

- [ ] **Step 3: Commit**

```bash
git add internal/parser/project_loader_test.go
git commit -m "test(parser): verify Paths service grouping by tag in ProjectLoader"
```

---

## Task 7: `Load` — verify PathImports and Model.Import populated

**Files:**
- Modify: `internal/parser/project_loader_test.go`

- [ ] **Step 1: Write the failing test**

Append:

```go
func TestProjectLoader_Load_PathImportsPopulated(t *testing.T) {
	pl := NewProjectLoader()
	ps, _, err := pl.Load("testdata/multiservice", nil,
		"nschugorev/oapigenerator/go", "/output")
	assert.NoError(t, err)

	userBackend := ps.ByName["userBackend"]
	pi := userBackend.Paths.Imports
	assert.Equal(t, "nschugorev/oapigenerator/go/userBackend/interfaces/client", pi.ClientInterfaces.Path)
	assert.Equal(t, "nschugorev/oapigenerator/go/userBackend/impl/httpclient", pi.ClientHTTP.Path)
	assert.Equal(t, "nschugorev/oapigenerator/go/userBackend/model", pi.Model.Path)
	assert.Equal(t, "nschugorev/oapigenerator/go/userBackend/sdk", pi.SDK.Path)

	// Model.Import
	assert.Equal(t, "nschugorev/oapigenerator/go/userBackend/model", userBackend.Model.Import.Path)
	assert.Equal(t, gogen.LocalImport, userBackend.Model.Import.Type)
}

func TestProjectLoader_Load_CommonPrefixSet(t *testing.T) {
	pl := NewProjectLoader()
	ps, _, err := pl.Load("testdata/multiservice", nil,
		"nschugorev/oapigenerator/go", "/output")
	assert.NoError(t, err)

	assert.Equal(t, "common", ps.Common.Model.Prefix,
		"common project must have Model.Prefix set for cross-service aliasing")
}
```

Add `"nschugorev/oapigenerator/internal/codegen/gogen"` to imports.

- [ ] **Step 2: Run to verify it passes**

Run: `go test ./internal/parser/ -run "TestProjectLoader_Load_PathImports|TestProjectLoader_Load_CommonPrefix" -v`
Expected: PASS — verifies `CreateModel`/`CreatePaths` factories were called with correct base paths.

- [ ] **Step 3: Commit**

```bash
git add internal/parser/project_loader_test.go
git commit -m "test(parser): verify PathImports and Model.Prefix in ProjectLoader"
```

---

## Task 8: `Load` — error cases (missing spec, duplicate folder)

**Files:**
- Modify: `internal/parser/project_loader_test.go`

- [ ] **Step 1: Write the failing test**

Append:

```go
func TestProjectLoader_Load_InputNotFound(t *testing.T) {
	pl := NewProjectLoader()
	_, _, err := pl.Load("testdata/does-not-exist", nil,
		"nschugorev/oapigenerator/go", "/output")
	assert.Error(t, err)
	assert.Contains(t, err.Error(), "walk services")
}

func TestProjectLoader_Load_MalformedSpec(t *testing.T) {
	// Создаём временный input с одним сервисом, spec — невалидный yaml
	tmp := t.TempDir()
	svcDir := filepath.Join(tmp, "broken", "src", "openapi")
	_ = os.MkdirAll(svcDir, 0o755)
	_ = os.WriteFile(filepath.Join(svcDir, "openapi.yaml"), []byte("not: valid: openapi"), 0o644)

	pl := NewProjectLoader()
	_, _, err := pl.Load(tmp, nil,
		"nschugorev/oapigenerator/go", "/output")
	assert.Error(t, err)
	assert.Contains(t, err.Error(), "load project")
}

func TestProjectLoader_Load_MultipleTagsError(t *testing.T) {
	tmp := t.TempDir()
	svcDir := filepath.Join(tmp, "multi", "src", "openapi")
	_ = os.MkdirAll(svcDir, 0o755)
	spec := []byte(`openapi: 3.0.3
info:
  title: Multi
  version: 1.0.0
paths:
  /x:
    get:
      operationId: X
      tags: [One, Two]
      responses:
        '200':
          description: ok
`)
	_ = os.WriteFile(filepath.Join(svcDir, "openapi.yaml"), spec, 0o644)

	pl := NewProjectLoader()
	_, _, err := pl.Load(tmp, nil,
		"nschugorev/oapigenerator/go", "/output")
	assert.Error(t, err)
	assert.Contains(t, err.Error(), "must be exactly one tag")
}
```

Add `"os"`, `"path/filepath"` to imports.

- [ ] **Step 2: Run to verify it passes**

Run: `go test ./internal/parser/ -run "TestProjectLoader_Load_InputNotFound|TestProjectLoader_Load_MalformedSpec|TestProjectLoader_Load_MultipleTagsError" -v`
Expected: PASS — verifies error propagation from walkServices, ParseFile, and serviceName.

- [ ] **Step 3: Commit**

```bash
git add internal/parser/project_loader_test.go
git commit -m "test(parser): verify ProjectLoader error cases"
```

---

## Task 9: `Load` — flagsLoader integration

**Files:**
- Create: `internal/parser/testdata/multiservice/generation_flags.yaml`
- Modify: `internal/parser/project_loader_test.go`

- [ ] **Step 1: Create global flags fixture**

`internal/parser/testdata/multiservice/generation_flags.yaml`:

```yaml
- name: GOLANG_SPLIT_REQUEST_RESPONSE
  default: false
- name: GOLANG_SERVER_BODY_REQUEST_NO_AUTO_DEFAULTS
  default: false
- name: USE_REQUIRED_V2
  default: false
- name: USE_UTC_FOR_DATE_TIME
  default: false
- name: GOLANG_USE_OPTIONAL
  default: false
```

- [ ] **Step 2: Write the failing test**

Append:

```go
func TestProjectLoader_Load_WithFlagsLoader(t *testing.T) {
	flagsLoader := NewGenerationFlagsLoader(fs.NewRealFS())
	err := flagsLoader.Load("testdata/multiservice/generation_flags.yaml")
	assert.NoError(t, err)

	pl := NewProjectLoader()
	ps, _, err := pl.Load("testdata/multiservice", flagsLoader,
		"nschugorev/oapigenerator/go", "/output")
	assert.NoError(t, err)

	// flagsLoader передан, но ни у одного сервиса нет generation_flags.yaml —
	// используются дефолты. Проверяем, что SplitRequestResponse = false.
	for _, p := range ps.Projects {
		assert.False(t, p.Features.SplitRequestResponse.Value,
			"project %q must have default features", p.Folder)
	}
}
```

Add `"nschugorev/oapigenerator/internal/fs"` to imports if not already present.

- [ ] **Step 3: Run to verify it passes**

Run: `go test ./internal/parser/ -run TestProjectLoader_Load_WithFlagsLoader -v`
Expected: PASS — verifies flagsLoader is threaded through and `resolveFeatures` returns defaults when `FlagsPath` is empty.

- [ ] **Step 4: Commit**

```bash
git add internal/parser/testdata/multiservice/generation_flags.yaml internal/parser/project_loader_test.go
git commit -m "test(parser): verify ProjectLoader flagsLoader integration"
```

---

## Task 10: Full test suite + vet + build verification

**Files:** None (verification only).

- [ ] **Step 1: Run full parser package tests**

Run: `go test ./internal/parser/ -v`
Expected: All tests PASS, including existing T26.1/T26.1a tests.

- [ ] **Step 2: Run full project build + vet**

Run: `go build ./... && go vet ./...`
Expected: Both clean.

- [ ] **Step 3: Run full test suite**

Run: `go test ./...`
Expected: All 15+ packages PASS.

- [ ] **Step 4: Verify no stale references**

Run: `grep -rn "ResourcesSet\|ResourceSchema" --include="*.go" internal/`
Expected: No matches (T26.1 already cleaned these; verify nothing regressed).

- [ ] **Step 5: Commit (if any cleanup needed)**

If verification surfaced fixes, commit them. Otherwise no commit.

---

## Self-Review Checklist (run after writing, before handoff)

**Spec coverage:**
- ✅ T26.2 steps 1-2 (walkServices + ParseFile) — Tasks 2, 4
- ✅ T26.2 step 3 (GetProjectFeatures) — Task 9
- ✅ T26.2 step 4 (Project construction) — Task 4
- ✅ T26.2 step 5 (CreateModel/CreatePaths) — Task 4, verified Task 7
- ✅ T26.2 step 6 (schema/operation transfer) — Task 4, verified Tasks 5-6
- ⏭️ T26.2 step 7 (markExternalRefs) — DEFERRED to T26.3 (user choice A)
- ⚠️ T26.2 step 8 (SchemaIndex) — returns empty index (deferred to T26.3)
- ✅ T26.2 step 9 (return ProjectSet + SchemaIndex) — Task 4

**Placeholder scan:** No TBD/TODO in tasks. All code blocks complete.

**Type consistency:** `serviceDescriptor`, `ProjectLoader`, `Load` signature consistent across tasks. `serviceName` called with `(op.Method, op.Path, op.Tags)` matching its T26.1a signature.

**Boundary clarity:** SchemaIndex returned but empty — explicitly documented in Scope boundaries. T26.3 will populate it after adding `Schema.SourceFile`.

---

## Execution Handoff

Plan saved to `docs/superpowers/plans/2026-07-15-t26.2-project-loader.md`. Two execution options:

1. **Subagent-Driven (recommended)** — fresh subagent per task + two-stage review
2. **Inline Execution** — batch in this session

Which approach?

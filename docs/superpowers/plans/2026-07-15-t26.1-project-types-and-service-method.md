# T26.1 + T26.1a: Project/SchemaIndex types + Service/Method abstraction

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.
>
> **Note on commits:** Per user preference, git commits are made by the user themselves. Commit steps below are guidance for the TDD rhythm — do NOT auto-commit. After each task, pause and let the user commit (or commit only when explicitly asked).

**Goal:** Rewrite the uncommitted `project_set.go`/`resources_set.go` (T26.1) with the rev2 design — `Project` aligned with `../api/mwsapigen` (`Folder`+`Model`+`Paths`+`Features`), `SchemaIndex` (renamed from `ResourcesSet`), `PathImports` with 8 typed `gogen.Import`. Then (T26.1a) introduce `Service`/`Method` abstraction, split `Document` into `Model.schemas` + `Paths.Services`.

**Architecture:** T26.1 = types + аксессоры + фабрики only (no loader logic). T26.1a = parser domain rework — rename `Operation`→`Method`, add `Service` grouping by tag, extend `Model`/`Paths`. Generator touched only for mechanical renames in T26.1a (keeps build green); full generator refactoring is T26.4.

**Spec:** `docs/superpowers/specs/2026-07-15-multi-service-layout-design-rev2.md`

**Tech Stack:** Go 1.22+, `github.com/stretchr/testify/assert`, `github.com/pb33f/libopenapi`, existing `internal/codegen/gogen` + `internal/codegen` packages.

**Simplification vs spec §1.4:** T26.1 `Paths` does NOT carry `document *Document` or `Operations()`/`PathItems()` helpers — they'd be dead code (no `ProjectLoader` until T26.2). T26.1a adds `Services` directly. `Project.Document` field is NOT added in T26.1 at all (spec said add-then-remove; we just never add).

---

## File Structure

### T26.1 — files touched

| File | Action | Responsibility |
|---|---|---|
| `internal/codegen/gogen/gogen.go` | Modify | Extend `Import` with `Package`+`Type`; add `ImportType`+`LocalImport` |
| `internal/parser/project_paths.go` | Create | `PathImports` struct (8 typed `gogen.Import`) |
| `internal/parser/model.go` | Create | Minimal `Model` (`Import`, `schemas`, `Schemas()`) |
| `internal/parser/paths_wrapper.go` | Create | Minimal `Paths` (`Imports PathImports`, `project` ref) |
| `internal/parser/project_set.go` | Rewrite | `Project` (new fields + `CreateModel`/`CreatePaths` factories), `ProjectSet`, `ByNameLookup` |
| `internal/parser/schema_index.go` | Create (rename from `resources_set.go`) | `SchemaIndex`, `SchemaEntry`, `LookupByFile`, `LookupForMode` |
| `internal/parser/resources_set.go` | Delete | Replaced by `schema_index.go` |
| `internal/parser/project_set_test.go` | Rewrite | Tests for new `Project`/`ProjectSet`/factories |
| `internal/parser/schema_index_test.go` | Create (rename from `resources_set_test.go`) | Tests for `SchemaIndex`/`SchemaEntry` |
| `internal/parser/resources_set_test.go` | Delete | Replaced by `schema_index_test.go` |

### T26.1a — files touched

| File | Action | Responsibility |
|---|---|---|
| `internal/parser/document.go` | Modify | Rename `Operation`→`Method`; `Document.Operations []*Method` |
| `internal/parser/service.go` | Create | `Service` struct (`Name`, `Methods`, `LowerName`) |
| `internal/parser/paths_wrapper.go` | Modify | Extend `Paths`: `Services`, `servicesMap`, `AddMethod`, `DeleteService` |
| `internal/parser/model.go` | Modify | Extend `Model`: `schemasIndex`, `Prefix`, `Lookup`, `Index` |
| `internal/parser/paths.go` | Modify | `convertOperation`→`convertMethod`, returns `*Method` |
| `internal/parser/parser.go` | Modify | Reference updates if any |
| `internal/parser/document.go` | Modify | `extractPaths` populates `[]*Method` |
| `internal/generator/*.go` | Modify (mechanical) | `*parser.Operation`→`*parser.Method` everywhere |
| Tests | Update | Parser + generator tests for `Method`/`Service` |

---

## T26.1 Tasks

### Task 1: Extend `gogen.Import` with `Package` + `Type`

**Files:**
- Modify: `internal/codegen/gogen/gogen.go:19-22`
- Test: `internal/codegen/gogen/gogen_test.go` (create if absent)

- [ ] **Step 1: Write the failing test**

Create `internal/codegen/gogen/gogen_test.go`:

```go
package gogen_test

import (
	"testing"

	"nschugorev/oapigenerator/internal/codegen/gogen"
	"github.com/stretchr/testify/assert"
)

func TestImport_PackageAndType(t *testing.T) {
	imp := gogen.Import{
		Path:    "nschugorev/oapigenerator/go/common/model",
		Alias:   "common",
		Package: "model",
		Type:    gogen.LocalImport,
	}
	assert.Equal(t, "model", imp.Package)
	assert.Equal(t, gogen.LocalImport, imp.Type)
}

func TestImportType_ZeroValue(t *testing.T) {
	var imp gogen.Import
	assert.Equal(t, gogen.ImportType(0), imp.Type)
	assert.Equal(t, gogen.LocalImport, imp.Type, "zero ImportType must equal LocalImport")
}
```

- [ ] **Step 2: Run test to verify it fails**

Run: `go test ./internal/codegen/gogen/ -run TestImport -v`
Expected: FAIL — `imp.Package undefined` / `imp.Type undefined` / `gogen.LocalImport undefined`.

- [ ] **Step 3: Extend `Import`**

Edit `internal/codegen/gogen/gogen.go` — replace the `Import` struct (lines 18-22) with:

```go
// ImportType классифицирует импорт: локальный (внутри модуля) или внешний.
// В первой итерации используется только LocalImport; ExternalImport и
// StdImport добавятся, когда категоризация при рендере станет нужна.
type ImportType int

const (
	// LocalImport — импорт внутри того же Go-модуля.
	LocalImport ImportType = iota
)

// Import описывает одну Go-импорт-спеку: путь, опциональный алиас, имя пакета
// и тип. Path и Alias используются рендером; Package и Type — семантические
// метки для потребителей (например, PathImports на Project).
type Import struct {
	Path    string
	Alias   string
	Package string // имя пакета для использования в коде ("client", "httpclient")
	Type    ImportType
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `go test ./internal/codegen/gogen/ -v`
Expected: PASS.

- [ ] **Step 5: Verify existing callers still compile**

Run: `go build ./...`
Expected: success (existing `Import{Path: ..., Alias: ...}` literals still valid — `Package`/`Type` zero-valued).

---

### Task 2: Create `PathImports` struct

**Files:**
- Create: `internal/parser/project_paths.go`
- Test: `internal/parser/project_paths_test.go`

- [ ] **Step 1: Write the failing test**

Create `internal/parser/project_paths_test.go`:

```go
package parser_test

import (
	"testing"

	"nschugorev/oapigenerator/internal/parser"
	"github.com/stretchr/testify/assert"
)

func TestPathImports_Fields(t *testing.T) {
	pi := parser.PathImports{
		ClientInterfaces: impForTest("nschugorev/oapigenerator/go/svc/interfaces/client", "client", ""),
		ServerInterfaces: impForTest("nschugorev/oapigenerator/go/svc/interfaces/server", "server", ""),
		ClientHTTP:       impForTest("nschugorev/oapigenerator/go/svc/impl/httpclient", "client", "http"),
		ServerHTTP:       impForTest("nschugorev/oapigenerator/go/svc/impl/echoserver", "server", "http"),
		ClientMocks:      impForTest("nschugorev/oapigenerator/go/svc/impl/mocks/client", "client", "mock"),
		ServerMocks:      impForTest("nschugorev/oapigenerator/go/svc/impl/mocks/server", "server", "mock"),
		Model:            impForTest("nschugorev/oapigenerator/go/svc/model", "", "model"),
		SDK:              impForTest("nschugorev/oapigenerator/go/svc/sdk", "", ""),
	}
	assert.Equal(t, "nschugorev/oapigenerator/go/svc/interfaces/client", pi.ClientInterfaces.Path)
	assert.Equal(t, "http", pi.ClientHTTP.Alias)
	assert.Equal(t, "model", pi.Model.Package)
}

// impForTest — хелпер для тестов, чтобы не тащить gogen.Import в каждый тест.
func impForTest(path, pkg, alias string) (out struct {
	Path    string
	Alias   string
	Package string
}) {
	return struct {
		Path    string
		Alias   string
		Package string
	}{Path: path, Alias: alias, Package: pkg}
}
```

Note: the helper uses an anonymous struct to avoid importing `gogen` in the test — adjust if `gogen` import is preferred. The real `PathImports` uses `gogen.Import`.

- [ ] **Step 2: Run test to verify it fails**

Run: `go test ./internal/parser/ -run TestPathImports -v`
Expected: FAIL — `parser.PathImports undefined`.

- [ ] **Step 3: Create `PathImports`**

Create `internal/parser/project_paths.go`:

```go
package parser

import "nschugorev/oapigenerator/internal/codegen/gogen"

// PathImports — типизированные Go-импорты артефактов одного сервиса.
// Создаётся один раз при Project.CreatePaths(basePath) и переиспользуется
// генератором вместо строковой конкатенации modulePath+"/<artifact>".
type PathImports struct {
	ClientInterfaces gogen.Import // <base>/interfaces/client
	ServerInterfaces gogen.Import // <base>/interfaces/server
	ClientHTTP       gogen.Import // <base>/impl/httpclient
	ServerHTTP       gogen.Import // <base>/impl/echoserver
	ClientMocks      gogen.Import // <base>/impl/mocks/client
	ServerMocks      gogen.Import // <base>/impl/mocks/server
	Model            gogen.Import // <base>/model
	SDK              gogen.Import // <base>/sdk
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `go test ./internal/parser/ -run TestPathImports -v`
Expected: PASS.

- [ ] **Step 5: Verify build**

Run: `go build ./...`
Expected: success.

---

### Task 3: Create minimal `Model`

**Files:**
- Create: `internal/parser/model.go`
- Test: `internal/parser/model_test.go`

- [ ] **Step 1: Write the failing test**

Create `internal/parser/model_test.go`:

```go
package parser_test

import (
	"testing"

	"nschugorev/oapigenerator/internal/codegen/gogen"
	"nschugorev/oapigenerator/internal/parser"
	"github.com/stretchr/testify/assert"
)

func TestModel_Schemas_Empty(t *testing.T) {
	m := &parser.Model{}
	assert.Nil(t, m.Schemas())
}

func TestModel_Schemas_ReturnsAssigned(t *testing.T) {
	schemas := []*parser.Schema{{Name: "User"}}
	m := &parser.Model{schemas: schemas}
	got := m.Schemas()
	assert.Len(t, got, 1)
	assert.Equal(t, "User", got[0].Name)
}

func TestModel_Import(t *testing.T) {
	m := &parser.Model{Import: gogen.Import{Path: "nschugorev/oapigenerator/go/svc/model", Package: "model"}}
	assert.Equal(t, "nschugorev/oapigenerator/go/svc/model", m.Import.Path)
}
```

Note: `Model.schemas` is unexported — the test is in `parser_test` (external). The test constructs `Model` via struct literal, but `schemas` is unexported. **Correction:** either make the test package `parser` (internal) or expose a setter. Use internal test package.

Change the test to `package parser` (internal):

```go
package parser

import (
	"testing"

	"nschugorev/oapigenerator/internal/codegen/gogen"
	"github.com/stretchr/testify/assert"
)

func TestModel_Schemas_Empty(t *testing.T) {
	m := &Model{}
	assert.Nil(t, m.Schemas())
}

func TestModel_Schemas_ReturnsAssigned(t *testing.T) {
	schemas := []*Schema{{Name: "User"}}
	m := &Model{schemas: schemas}
	got := m.Schemas()
	assert.Len(t, got, 1)
	assert.Equal(t, "User", got[0].Name)
}

func TestModel_Import(t *testing.T) {
	m := &Model{Import: gogen.Import{Path: "nschugorev/oapigenerator/go/svc/model", Package: "model"}}
	assert.Equal(t, "nschugorev/oapigenerator/go/svc/model", m.Import.Path)
}
```

- [ ] **Step 2: Run test to verify it fails**

Run: `go test ./internal/parser/ -run TestModel -v`
Expected: FAIL — `parser.Model undefined` / `Model undefined`.

- [ ] **Step 3: Create `Model`**

Create `internal/parser/model.go`:

```go
package parser

import "nschugorev/oapigenerator/internal/codegen/gogen"

// Model — доменная модель схем сервиса. В T26.1 минимальна:
// обёртка над schemas + Import. В T26.1a расширится (schemasIndex, Prefix,
// Lookup, Index).
type Model struct {
	project *Project
	Import  gogen.Import
	schemas []*Schema // в T26.1 не заполняется; в T26.2 ProjectLoader переносит doc.Schemas
}

// Schemas возвращает схемы сервиса. До заполнения ProjectLoader'ом — nil.
func (m *Model) Schemas() []*Schema { return m.schemas }
```

- [ ] **Step 4: Run test to verify it passes**

Run: `go test ./internal/parser/ -run TestModel -v`
Expected: PASS.

- [ ] **Step 5: Verify build**

Run: `go build ./...`
Expected: success.

---

### Task 4: Create minimal `Paths`

**Files:**
- Create: `internal/parser/paths_wrapper.go`
- Test: `internal/parser/paths_wrapper_test.go`

- [ ] **Step 1: Write the failing test**

Create `internal/parser/paths_wrapper_test.go` (internal package):

```go
package parser

import (
	"testing"

	"github.com/stretchr/testify/assert"
)

func TestPaths_ImportsZeroValue(t *testing.T) {
	p := &Paths{}
	// zero-value Paths has empty PathImports
	assert.Equal(t, "", p.Imports.ClientInterfaces.Path)
}

func TestPaths_ImportsAssigned(t *testing.T) {
	pi := PathImports{}
	pi.ClientHTTP.Path = "nschugorev/oapigenerator/go/svc/impl/httpclient"
	p := &Paths{Imports: pi}
	assert.Equal(t, "nschugorev/oapigenerator/go/svc/impl/httpclient", p.Imports.ClientHTTP.Path)
}
```

- [ ] **Step 2: Run test to verify it fails**

Run: `go test ./internal/parser/ -run TestPaths -v`
Expected: FAIL — `parser.Paths undefined`.

- [ ] **Step 3: Create `Paths`**

Create `internal/parser/paths_wrapper.go`:

```go
package parser

// Paths — доменная модель операций сервиса. В T26.1: только PathImports +
// ссылка на Project. В T26.1a — Services []*Service с Methods.
type Paths struct {
	Imports PathImports
	project *Project
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `go test ./internal/parser/ -run TestPaths -v`
Expected: PASS.

- [ ] **Step 5: Verify build**

Run: `go build ./...`
Expected: success.

---

### Task 5: Rewrite `Project` + `ProjectSet` with factories

**Files:**
- Rewrite: `internal/parser/project_set.go`
- Rewrite: `internal/parser/project_set_test.go`

- [ ] **Step 1: Write the failing test**

Replace `internal/parser/project_set_test.go` entirely:

```go
package parser_test

import (
	"testing"

	"nschugorev/oapigenerator/internal/codegen/gogen"
	"nschugorev/oapigenerator/internal/parser"
	"github.com/stretchr/testify/assert"
)

func TestProjectSet_ByNameLookup(t *testing.T) {
	common := &parser.Project{Folder: "common"}
	userBackend := &parser.Project{Folder: "userBackend"}

	ps := &parser.ProjectSet{
		Common:   common,
		Projects: []*parser.Project{common, userBackend},
		ByName:   map[string]*parser.Project{"common": common, "userBackend": userBackend},
	}

	got, ok := ps.ByNameLookup("userBackend")
	assert.True(t, ok)
	assert.Same(t, userBackend, got)

	_, ok = ps.ByNameLookup("nonexistent")
	assert.False(t, ok)
}

func TestProject_Folder(t *testing.T) {
	p := &parser.Project{
		Folder:       "userBackend",
		SpecPath:     "/input/userBackend/src/openapi/openapi.yaml",
		FlagsPath:    "/input/userBackend/generation_flags.yaml",
		OutputDir:    "/go/userBackend",
		ImportPrefix: "nschugorev/oapigenerator/go/userBackend",
	}
	assert.Equal(t, "userBackend", p.Folder)
	assert.Equal(t, "/go/userBackend", p.OutputDir)
}

func TestProject_CreateModel(t *testing.T) {
	p := &parser.Project{Folder: "svc"}
	imp := gogen.Import{Path: "nschugorev/oapigenerator/go/svc/model", Package: "model"}
	m := p.CreateModel(imp)

	assert.NotNil(t, p.Model)
	assert.Same(t, m, p.Model)
	assert.Equal(t, "nschugorev/oapigenerator/go/svc/model", p.Model.Import.Path)
	assert.Equal(t, gogen.LocalImport, p.Model.Import.Type, "CreateModel must set Type to LocalImport")
}

func TestProject_CreatePaths(t *testing.T) {
	p := &parser.Project{Folder: "svc"}
	pi := p.CreatePaths("nschugorev/oapigenerator/go/svc")

	assert.NotNil(t, p.Paths)
	assert.Same(t, pi, p.Paths)

	// 8 typed imports populated.
	assert.Equal(t, "nschugorev/oapigenerator/go/svc/interfaces/client", pi.Imports.ClientInterfaces.Path)
	assert.Equal(t, "nschugorev/oapigenerator/go/svc/interfaces/server", pi.Imports.ServerInterfaces.Path)
	assert.Equal(t, "nschugorev/oapigenerator/go/svc/impl/httpclient", pi.Imports.ClientHTTP.Path)
	assert.Equal(t, "nschugorev/oapigenerator/go/svc/impl/echoserver", pi.Imports.ServerHTTP.Path)
	assert.Equal(t, "nschugorev/oapigenerator/go/svc/impl/mocks/client", pi.Imports.ClientMocks.Path)
	assert.Equal(t, "nschugorev/oapigenerator/go/svc/impl/mocks/server", pi.Imports.ServerMocks.Path)
	assert.Equal(t, "nschugorev/oapigenerator/go/svc/model", pi.Imports.Model.Path)
	assert.Equal(t, "nschugorev/oapigenerator/go/svc/sdk", pi.Imports.SDK.Path)

	// Aliases.
	assert.Equal(t, "http", pi.Imports.ClientHTTP.Alias)
	assert.Equal(t, "http", pi.Imports.ServerHTTP.Alias)
	assert.Equal(t, "mock", pi.Imports.ClientMocks.Alias)
	assert.Equal(t, "mock", pi.Imports.ServerMocks.Alias)
	assert.Equal(t, "model", pi.Imports.Model.Alias)

	// Packages.
	assert.Equal(t, "client", pi.Imports.ClientInterfaces.Package)
	assert.Equal(t, "client", pi.Imports.ClientHTTP.Package)

	// Type.
	assert.Equal(t, gogen.LocalImport, pi.Imports.SDK.Type)
}
```

- [ ] **Step 2: Run test to verify it fails**

Run: `go test ./internal/parser/ -run TestProject -v`
Expected: FAIL — `parser.Project` has no `Folder`/`CreateModel`/`CreatePaths`.

- [ ] **Step 3: Rewrite `Project`**

Replace `internal/parser/project_set.go` entirely:

```go
package parser

import "nschugorev/oapigenerator/internal/codegen/gogen"

// Project — один сервис в составе project layout'а. Содержит Folder,
// абсолютные пути к спеке и флагам, резолвнутые generation flags, и пути
// вывода. Model и Paths — доменные модели схем и операций, создаются
// фабриками CreateModel/CreatePaths.
type Project struct {
	Folder       string          // относительный путь от input ("userBackend", "common", "category/userService")
	SpecPath     string          // абсолютный путь к src/openapi/openapi.yaml
	FlagsPath    string          // абсолютный путь к generation_flags.yaml
	Features     ProjectFeatures // резолвнутые флаги (global + per-service override)
	Model        *Model          // доменная модель схем; nil до CreateModel
	Paths        *Paths          // доменная модель операций; nil до CreatePaths
	OutputDir    string          // <output>/<folder>
	ImportPrefix string          // <import-prefix>/<folder> — основа для PathImports
}

// ProjectSet — коллекция всех сервисов проекта.
type ProjectSet struct {
	Common   *Project            // nil если common не найден
	Projects []*Project          // все проекты включая common, для итерации
	ByName   map[string]*Project // индекс по Folder
}

// ByNameLookup возвращает проект по имени. Второе возвращаемое — false если не найден.
func (ps *ProjectSet) ByNameLookup(name string) (*Project, bool) {
	p, ok := ps.ByName[name]
	return p, ok
}

// CreateModel создаёт Model с заданным импортом и привязывает к Project.
// imp.Type принудительно устанавливается в LocalImport.
func (p *Project) CreateModel(imp gogen.Import) *Model {
	imp.Type = gogen.LocalImport
	m := &Model{project: p, Import: imp}
	p.Model = m
	return m
}

// CreatePaths создаёт Paths с типизированными PathImports для всех артефактов
// и привязывает к Project. basePath — Go import path корня сервиса
// (например "nschugorev/oapigenerator/go/userBackend").
func (p *Project) CreatePaths(basePath string) *Paths {
	imp := func(sub, pkg, alias string) gogen.Import {
		return gogen.Import{
			Path:    basePath + "/" + sub,
			Alias:   alias,
			Package: pkg,
			Type:    gogen.LocalImport,
		}
	}
	pi := &Paths{
		Imports: PathImports{
			ClientInterfaces: imp("interfaces/client", "client", ""),
			ServerInterfaces: imp("interfaces/server", "server", ""),
			ClientHTTP:       imp("impl/httpclient", "client", "http"),
			ServerHTTP:       imp("impl/echoserver", "server", "http"),
			ClientMocks:      imp("impl/mocks/client", "client", "mock"),
			ServerMocks:      imp("impl/mocks/server", "server", "mock"),
			Model:            imp("model", "", "model"),
			SDK:              imp("sdk", "", ""),
		},
		project: p,
	}
	p.Paths = pi
	return pi
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `go test ./internal/parser/ -run TestProject -v`
Expected: PASS.

- [ ] **Step 5: Verify full build**

Run: `go build ./...`
Expected: success. (No existing callers of old `Project.Name` remain — current codebase doesn't use `Project` yet outside these uncommitted files.)

---

### Task 6: Rename `ResourcesSet` → `SchemaIndex`

**Files:**
- Create: `internal/parser/schema_index.go` (rename from `resources_set.go`)
- Create: `internal/parser/schema_index_test.go` (rename from `resources_set_test.go`)
- Delete: `internal/parser/resources_set.go`, `internal/parser/resources_set_test.go`

- [ ] **Step 1: Write the failing test**

Create `internal/parser/schema_index_test.go` (replaces `resources_set_test.go`):

```go
package parser_test

import (
	"testing"

	"nschugorev/oapigenerator/internal/parser"
	"github.com/stretchr/testify/assert"
)

func TestSchemaIndex_LookupByFile(t *testing.T) {
	common := &parser.Project{Folder: "common", ImportPrefix: "nschugorev/oapigenerator/go/common"}
	si := &parser.SchemaIndex{
		Schemas: map[string]*parser.SchemaEntry{
			"/input/common/src/openapi/schemas/User.yaml": {
				Project:    common,
				SchemaName: "User",
				GoImport:   "nschugorev/oapigenerator/go/common",
				GoType:     "User",
			},
		},
	}

	got, ok := si.LookupByFile("/input/common/src/openapi/schemas/User.yaml")
	assert.True(t, ok)
	assert.Equal(t, "User", got.GoType)
	assert.Equal(t, "nschugorev/oapigenerator/go/common", got.GoImport)

	_, ok = si.LookupByFile("/nonexistent.yaml")
	assert.False(t, ok)
}

func TestSchemaIndex_LookupForMode_NoSplit(t *testing.T) {
	common := &parser.Project{Folder: "common", ImportPrefix: "nschugorev/oapigenerator/go/common"}
	si := &parser.SchemaIndex{
		Schemas: map[string]*parser.SchemaEntry{
			"/input/common/src/openapi/schemas/User.yaml": {
				Project:    common,
				SchemaName: "User",
				GoImport:   "nschugorev/oapigenerator/go/common",
				GoType:     "User",
			},
		},
	}

	// Моно режим → User.
	got, ok := si.LookupForMode("/input/common/src/openapi/schemas/User.yaml", "")
	assert.True(t, ok)
	assert.Equal(t, "User", got.GoType)

	// Request режим, но split НЕ включён во владельце → GoType не меняется.
	got, ok = si.LookupForMode("/input/common/src/openapi/schemas/User.yaml", parser.ModeRequest)
	assert.True(t, ok)
	assert.Equal(t, "User", got.GoType)
}

func TestSchemaIndex_LookupForMode_SplitEnabled(t *testing.T) {
	common := &parser.Project{
		Folder:       "common",
		ImportPrefix: "nschugorev/oapigenerator/go/common",
		Features: parser.ProjectFeatures{
			SplitRequestResponse: parser.ProjectFeature{Value: true},
		},
	}
	const key = "/input/common/src/openapi/schemas/User.yaml"
	si := &parser.SchemaIndex{
		Schemas: map[string]*parser.SchemaEntry{
			key: {
				Project:    common,
				SchemaName: "User",
				GoImport:   "nschugorev/oapigenerator/go/common",
				GoType:     "User",
			},
		},
	}

	// Request mode → UserRequest.
	got, ok := si.LookupForMode(key, parser.ModeRequest)
	assert.True(t, ok)
	assert.Equal(t, "UserRequest", got.GoType)

	// Response mode → UserResponse.
	got, ok = si.LookupForMode(key, parser.ModeResponse)
	assert.True(t, ok)
	assert.Equal(t, "UserResponse", got.GoType)

	// Mono mode → User (no suffix).
	got, ok = si.LookupForMode(key, "")
	assert.True(t, ok)
	assert.Equal(t, "User", got.GoType)

	// Index entry must not be mutated.
	assert.Equal(t, "User", si.Schemas[key].GoType,
		"LookupForMode must return a copy and not mutate the index entry")
}
```

- [ ] **Step 2: Delete old files**

```bash
rm internal/parser/resources_set.go internal/parser/resources_set_test.go
```

- [ ] **Step 3: Run test to verify it fails**

Run: `go test ./internal/parser/ -run TestSchemaIndex -v`
Expected: FAIL — `parser.SchemaIndex undefined`.

- [ ] **Step 4: Create `SchemaIndex`**

Create `internal/parser/schema_index.go`:

```go
package parser

// Mode constants for LookupForMode. Match the conventions used by the
// generator package (internal/generator/constants.go).
const (
	ModeRequest  = "Request"
	ModeResponse = "Response"
)

// SchemaIndex — глобальный индекс схем всех сервисов. Ключ — абсолютный
// путь к yaml-файлу схемы. Используется генератором для разрешения
// cross-service $ref: вместо генерации дубликата типа в текущем сервисе,
// эмитится Go-импорт в пакет сервиса-владельца.
type SchemaIndex struct {
	Schemas map[string]*SchemaEntry
}

// SchemaEntry — запись в индексе: где лежит схема, какой Go-импорт и
// имя типа использовать при cross-service ссылках.
type SchemaEntry struct {
	Project    *Project
	SchemaName string // имя схемы во владельце (для диагностики)
	GoImport   string // например "nschugorev/oapigenerator/go/common"
	GoType     string // например "User"; с учётом split-mode: "UserRequest"/"UserResponse"
}

// LookupByFile возвращает SchemaEntry по абсолютному пути к yaml-файлу.
// Второе возвращаемое — false если путь не зарегистрирован.
func (si *SchemaIndex) LookupByFile(absPath string) (*SchemaEntry, bool) {
	e, ok := si.Schemas[absPath]
	return e, ok
}

// LookupForMode возвращает SchemaEntry с GoType, адаптированным под mode
// текущего использования ("", ModeRequest, ModeResponse). Если во владельце
// не включён split-mode, GoType возвращается как есть.
func (si *SchemaIndex) LookupForMode(absPath, mode string) (*SchemaEntry, bool) {
	e, ok := si.LookupByFile(absPath)
	if !ok {
		return nil, false
	}

	if e.Project == nil || !e.Project.Features.SplitRequestResponse.Value {
		return e, true
	}

	// Split включён — суффиксуем тип в зависимости от mode.
	out := *e
	switch mode {
	case ModeRequest:
		out.GoType = e.GoType + "Request"
	case ModeResponse:
		out.GoType = e.GoType + "Response"
	}
	return &out, true
}
```

- [ ] **Step 5: Run test to verify it passes**

Run: `go test ./internal/parser/ -run TestSchemaIndex -v`
Expected: PASS.

- [ ] **Step 6: Verify full build + all parser tests**

Run: `go build ./... && go test ./internal/parser/ ./internal/codegen/gogen/ -v`
Expected: success + all PASS.

---

## T26.1a Tasks

### Task 7: Rename `Operation` → `Method` in `document.go`

**Files:**
- Modify: `internal/parser/document.go:42-53` (struct), `document.go:19` (Document.Operations field type)
- Modify: `internal/parser/paths.go:35-67` (`convertOperation` → `convertMethod`)
- Modify: all `internal/generator/*.go` files referencing `*parser.Operation`
- Tests: update references

- [ ] **Step 1: Find all references to `Operation`**

Run: `grep -rn "parser\.Operation\|\.Operation\b" internal/ --include="*.go" | grep -v "_test.go" | grep -v "Operations\b"`

Note: `Operation` the type vs `Operations` the slice field vs `op.OperationID` the field. Only rename the **type** `Operation` → `Method`. `OperationID` field stays. `Document.Operations` field stays (slice of `*Method` now).

- [ ] **Step 2: Rename the struct in `document.go`**

Edit `internal/parser/document.go`:

Line 19: `Operations []*Operation` → `Operations []*Method`

Lines 41-53: replace the `Operation` struct:

```go
// Method — HTTP-операция (переименовано из Operation в T26.1a для
// соответствия эталону ../api/mwsapigen и будущей Service/Method абстракции).
type Method struct {
	Method      string
	Path        string
	OperationID string
	Summary     string
	Description string
	Tags        []string
	Deprecated  bool
	Parameters  []*Parameter
	RequestBody *RequestBody
	Responses   []*Response

	service *Service // back-reference, выставляется при Paths.AddMethod
}

// ServiceName возвращает имя сервиса (тег), которому принадлежит метод.
// Nil-safe: возвращает "" если method ещё не привязан к Service.
func (m *Method) ServiceName() string {
	if m == nil || m.service == nil {
		return ""
	}
	return m.service.Name
}

// Name возвращает Go-имя метода (OperationID). Если OperationID пустой —
// возвращает пустую строку; валидация пустых OperationID — ответственность
// парсера (см. convertMethod).
func (m *Method) Name() string { return m.OperationID }
```

- [ ] **Step 3: Rename `convertOperation` → `convertMethod` in `paths.go`**

Edit `internal/parser/paths.go`:

Line 25: `op := convertOperation(pathStr, opPair.Key(), opPair.Value())` → `op := convertMethod(pathStr, opPair.Key(), opPair.Value())`

Lines 35-67: rename function:

```go
func convertMethod(path, method string, op *highv3.Operation) *Method {
	out := &Method{
		Method:      strings.ToUpper(method),
		Path:        path,
		OperationID: op.OperationId,
		Summary:     op.Summary,
		Description: op.Description,
		Tags:        append([]string(nil), op.Tags...),
		Deprecated:  boolPtrOrFalse(op.Deprecated),
	}

	for _, p := range op.Parameters {
		out.Parameters = append(out.Parameters, convertParameter(p))
	}

	if op.RequestBody != nil {
		out.RequestBody = convertRequestBody(op.RequestBody)
	}

	if op.Responses != nil {
		if op.Responses.Default != nil {
			out.Responses = append(out.Responses, convertResponse("default", op.Responses.Default))
		}

		if op.Responses.Codes != nil {
			for codePair := op.Responses.Codes.First(); codePair != nil; codePair = codePair.Next() {
				out.Responses = append(out.Responses, convertResponse(codePair.Key(), codePair.Value()))
			}
		}
	}

	return out
}
```

- [ ] **Step 4: Rename all generator references**

Run a mechanical rename across `internal/generator/`:

```bash
# Replace parser.Operation with parser.Method in non-test files.
find internal/generator -name "*.go" ! -name "*_test.go" -exec sed -i '' 's/parser\.Operation/parser.Method/g' {} +

# In generator function/variable names: operationMethodName stays (it's about HTTP method, not the type).
# But parameter types like `op *parser.Operation` → `op *parser.Method`.
```

Verify with: `grep -rn "parser\.Operation\b" internal/generator/ --include="*.go"`
Expected: no matches.

Also update test files:

```bash
find internal/generator -name "*_test.go" -exec sed -i '' 's/parser\.Operation/parser.Method/g' {} +
find internal/parser -name "*_test.go" -exec sed -i '' 's/\bOperation\b/Method/g' {} +
```

**Caution:** `sed` on test files may also rename `OperationID` → `MethodID`. Use word boundaries carefully:

```bash
# Only rename standalone "Operation" type references, not "OperationID" or "Operations".
find internal/parser -name "*_test.go" -exec sed -i '' 's/\*parser\.Operation\b/*parser.Method/g; s/parser\.Operation\b/parser.Method/g' {} +
```

- [ ] **Step 5: Verify build**

Run: `go build ./...`
Expected: success. If failures — fix remaining `Operation` references manually (check `internal/parser/parser_test.go`, `internal/parser/internal_test.go`).

- [ ] **Step 6: Run all tests**

Run: `go test ./...`
Expected: all PASS (existing tests unchanged in behavior — pure rename).

---

### Task 8: Create `Service` struct + tag-based grouping

**Files:**
- Create: `internal/parser/service.go`
- Test: `internal/parser/service_test.go`

- [ ] **Step 1: Write the failing test**

Create `internal/parser/service_test.go` (internal package for access to unexported fields):

```go
package parser

import (
	"testing"

	"github.com/stretchr/testify/assert"
)

func TestService_LowerName(t *testing.T) {
	s := &Service{Name: "UserBackend"}
	assert.Equal(t, "userbackend", s.LowerName())
}

func TestService_Empty(t *testing.T) {
	s := &Service{}
	assert.Equal(t, "", s.LowerName())
}
```

- [ ] **Step 2: Run test to verify it fails**

Run: `go test ./internal/parser/ -run TestService -v`
Expected: FAIL — `parser.Service undefined`.

- [ ] **Step 3: Create `Service`**

Create `internal/parser/service.go`:

```go
package parser

import "strings"

// Service — группа методов, объединённых по тегу (op.Tags[0]).
// Используется генератором для эмитции server interfaces по сервисам.
type Service struct {
	Name    string
	Methods []*Method

	paths *Paths // back-reference
}

// LowerName возвращает имя сервиса в нижнем регистре — используется для
// имен файлов и Go-идентификаторов где важен lowercase.
func (s *Service) LowerName() string {
	return strings.ToLower(s.Name)
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `go test ./internal/parser/ -run TestService -v`
Expected: PASS.

- [ ] **Step 5: Verify build**

Run: `go build ./...`
Expected: success.

---

### Task 9: Extend `Paths` with `Services` + `AddMethod` + `DeleteService`

**Files:**
- Modify: `internal/parser/paths_wrapper.go`
- Test: `internal/parser/paths_wrapper_test.go` (extend)

- [ ] **Step 1: Write the failing test**

Replace `internal/parser/paths_wrapper_test.go`:

```go
package parser

import (
	"testing"

	"github.com/stretchr/testify/assert"
)

func TestPaths_ImportsZeroValue(t *testing.T) {
	p := &Paths{}
	assert.Equal(t, "", p.Imports.ClientInterfaces.Path)
}

func TestPaths_AddMethod_NewService(t *testing.T) {
	p := &Paths{}
	m1 := &Method{OperationID: "GetUser"}
	m2 := &Method{OperationID: "CreateUser"}

	p.AddMethod("UserBackend", m1)
	p.AddMethod("UserBackend", m2)

	assert.Len(t, p.Services, 1)
	assert.Equal(t, "UserBackend", p.Services[0].Name)
	assert.Len(t, p.Services[0].Methods, 2)
	assert.Same(t, p.Services[0], m1.service, "AddMethod must set method.service back-ref")
}

func TestPaths_AddMethod_MultipleServices(t *testing.T) {
	p := &Paths{}
	p.AddMethod("UserBackend", &Method{OperationID: "GetUser"})
	p.AddMethod("AuthBackend", &Method{OperationID: "Login"})

	assert.Len(t, p.Services, 2)
	assert.Equal(t, "UserBackend", p.Services[0].Name)
	assert.Equal(t, "AuthBackend", p.Services[1].Name)
}

func TestPaths_DeleteService(t *testing.T) {
	p := &Paths{}
	p.AddMethod("UserBackend", &Method{OperationID: "GetUser"})
	p.AddMethod("AuthBackend", &Method{OperationID: "Login"})

	p.DeleteService("UserBackend")

	assert.Len(t, p.Services, 1)
	assert.Equal(t, "AuthBackend", p.Services[0].Name)
	assert.NotContains(t, p.servicesMap, "UserBackend")
}
```

- [ ] **Step 2: Run test to verify it fails**

Run: `go test ./internal/parser/ -run TestPaths -v`
Expected: FAIL — `p.Services undefined`, `p.AddMethod undefined`.

- [ ] **Step 3: Extend `Paths`**

Replace `internal/parser/paths_wrapper.go`:

```go
package parser

import "slices"

// Paths — доменная модель операций сервиса. Содержит PathImports (типизированные
// Go-импорты артефактов) и Services (методы, сгруппированные по тегу).
type Paths struct {
	Imports  PathImports
	Services []*Service

	servicesMap map[string]*Service
	project     *Project
}

// AddMethod привязывает метод к сервису с заданным именем. Если сервис
// ещё не существует — создаёт. Выставляет method.service back-reference.
func (p *Paths) AddMethod(serviceName string, m *Method) {
	if p.servicesMap == nil {
		p.servicesMap = map[string]*Service{}
	}
	s := p.servicesMap[serviceName]
	if s == nil {
		s = &Service{Name: serviceName, paths: p}
		p.servicesMap[serviceName] = s
		p.Services = append(p.Services, s)
	}
	s.Methods = append(s.Methods, m)
	m.service = s
}

// DeleteService удаляет сервис по имени. Используется в future public-spec
// модификациях (T22, бэклог). No-op если сервис не найден.
func (p *Paths) DeleteService(name string) {
	delete(p.servicesMap, name)
	p.Services = slices.DeleteFunc(p.Services, func(s *Service) bool {
		return s.Name == name
	})
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `go test ./internal/parser/ -run TestPaths -v`
Expected: PASS.

- [ ] **Step 5: Verify build**

Run: `go build ./...`
Expected: success.

---

### Task 10: Extend `Model` with `schemasIndex` + `Prefix` + `Lookup` + `Index`

**Files:**
- Modify: `internal/parser/model.go`
- Test: `internal/parser/model_test.go` (extend)

- [ ] **Step 1: Write the failing test**

Replace `internal/parser/model_test.go`:

```go
package parser

import (
	"testing"

	"nschugorev/oapigenerator/internal/codegen/gogen"
	"github.com/stretchr/testify/assert"
)

func TestModel_Schemas_Empty(t *testing.T) {
	m := &Model{}
	assert.Nil(t, m.Schemas())
}

func TestModel_Schemas_ReturnsAssigned(t *testing.T) {
	schemas := []*Schema{{Name: "User"}}
	m := &Model{schemas: schemas}
	got := m.Schemas()
	assert.Len(t, got, 1)
	assert.Equal(t, "User", got[0].Name)
}

func TestModel_Import(t *testing.T) {
	m := &Model{Import: gogen.Import{Path: "nschugorev/oapigenerator/go/svc/model", Package: "model"}}
	assert.Equal(t, "nschugorev/oapigenerator/go/svc/model", m.Import.Path)
}

func TestModel_Lookup_BeforeIndex(t *testing.T) {
	m := &Model{schemas: []*Schema{{Name: "User"}}}
	_, ok := m.Lookup("User")
	assert.False(t, ok, "Lookup must return false before Index() is called")
}

func TestModel_Index_AndLookup(t *testing.T) {
	m := &Model{schemas: []*Schema{{Name: "User"}, {Name: "Profile"}}}
	m.Index()

	got, ok := m.Lookup("User")
	assert.True(t, ok)
	assert.Equal(t, "User", got.Name)

	got, ok = m.Lookup("Profile")
	assert.True(t, ok)
	assert.Equal(t, "Profile", got.Name)

	_, ok = m.Lookup("Nonexistent")
	assert.False(t, ok)
}

func TestModel_Prefix(t *testing.T) {
	m := &Model{Prefix: "common"}
	assert.Equal(t, "common", m.Prefix)
}
```

- [ ] **Step 2: Run test to verify it fails**

Run: `go test ./internal/parser/ -run TestModel -v`
Expected: FAIL — `m.Lookup undefined`, `m.Index undefined`, `m.Prefix undefined`.

- [ ] **Step 3: Extend `Model`**

Replace `internal/parser/model.go`:

```go
package parser

import "nschugorev/oapigenerator/internal/codegen/gogen"

// Model — доменная модель схем сервиса. Содержит schemas, Import (Go-импорт
// model-пакета), Prefix (alias для common-проекта), и schemasIndex для
// быстрого Lookup по имени.
type Model struct {
	project      *Project
	Import       gogen.Import
	schemas      []*Schema
	schemasIndex map[string]*Schema // строится Index()
	Prefix       string             // для common: "common" alias в импортах
}

// Schemas возвращает схемы сервиса. До заполнения ProjectLoader'ом — nil.
func (m *Model) Schemas() []*Schema { return m.schemas }

// Index строит schemasIndex по m.schemas. Идемпотентен. Должен вызываться
// после заполнения schemas (в ProjectLoader.Load).
func (m *Model) Index() {
	m.schemasIndex = make(map[string]*Schema, len(m.schemas))
	for _, s := range m.schemas {
		if s.Name == "" {
			continue
		}
		m.schemasIndex[s.Name] = s
	}
}

// Lookup возвращает схему по имени. Требует предварительного вызова Index().
// Второе возвращаемое — false если Index не вызван или имя не найдено.
func (m *Model) Lookup(name string) (*Schema, bool) {
	if m.schemasIndex == nil {
		return nil, false
	}
	s, ok := m.schemasIndex[name]
	return s, ok
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `go test ./internal/parser/ -run TestModel -v`
Expected: PASS.

- [ ] **Step 5: Verify build**

Run: `go build ./...`
Expected: success.

---

### Task 11: Add tag validation helper `serviceName`

**Files:**
- Modify: `internal/parser/paths.go` (add `serviceName` function)
- Test: `internal/parser/service_name_test.go`

- [ ] **Step 1: Write the failing test**

Create `internal/parser/service_name_test.go`:

```go
package parser

import (
	"testing"

	"github.com/stretchr/testify/assert"
)

func TestServiceName_NoTags(t *testing.T) {
	got, err := serviceName("GET", "/users", nil)
	assert.NoError(t, err)
	assert.Equal(t, "Service", got)
}

func TestServiceName_SingleTag(t *testing.T) {
	got, err := serviceName("GET", "/users", []string{"UserBackend"})
	assert.NoError(t, err)
	assert.Equal(t, "UserBackend", got)
}

func TestServiceName_MultipleTags(t *testing.T) {
	_, err := serviceName("GET", "/users", []string{"UserBackend", "Admin"})
	assert.Error(t, err)
	assert.Contains(t, err.Error(), "Must be exactly one tag")
}

func TestServiceName_EmptyTag(t *testing.T) {
	_, err := serviceName("GET", "/users", []string{""})
	assert.Error(t, err)
	assert.Contains(t, err.Error(), "Tag must not be empty")
}
```

- [ ] **Step 2: Run test to verify it fails**

Run: `go test ./internal/parser/ -run TestServiceName -v`
Expected: FAIL — `serviceName undefined`.

- [ ] **Step 3: Implement `serviceName`**

Add to `internal/parser/paths.go` (after `convertMethod`):

```go
import "github.com/go-errors/errors"

// serviceName определяет имя сервиса из тегов операции. Правила (соответствие
// эталону ../api/mwsapigen/internal/parser/loader.go:271):
//   - 0 тегов → "Service" (дефолт).
//   - >1 тега → error (должен быть ровно один тег).
//   - пустой тег → error.
func serviceName(method, path string, tags []string) (string, error) {
	if len(tags) == 0 {
		return "Service", nil
	}
	if len(tags) != 1 {
		return "", errors.Errorf("Must be exactly one tag for %s : %s", path, method)
	}
	tag := tags[0]
	if tag == "" {
		return "", errors.Errorf("Tag must not be empty for %s : %s", path, method)
	}
	return tag, nil
}
```

Note: check if `github.com/go-errors/errors` is already a dependency. If not, use `fmt.Errorf` instead:

```go
import "fmt"

func serviceName(method, path string, tags []string) (string, error) {
	if len(tags) == 0 {
		return "Service", nil
	}
	if len(tags) != 1 {
		return "", fmt.Errorf("must be exactly one tag for %s : %s", path, method)
	}
	tag := tags[0]
	if tag == "" {
		return "", fmt.Errorf("tag must not be empty for %s : %s", path, method)
	}
	return tag, nil
}
```

Use the `fmt.Errorf` version to avoid adding a dependency. Adjust the test's `Contains` strings to match lowercase.

- [ ] **Step 4: Run test to verify it passes**

Run: `go test ./internal/parser/ -run TestServiceName -v`
Expected: PASS.

- [ ] **Step 5: Verify build**

Run: `go build ./...`
Expected: success.

---

### Task 12: Final verification + full test suite

**Files:** none (verification only)

- [ ] **Step 1: Run full test suite**

Run: `go test ./...`
Expected: all PASS.

- [ ] **Step 2: Run vet**

Run: `go vet ./...`
Expected: no issues.

- [ ] **Step 3: Verify no stale references**

Run: `grep -rn "ResourcesSet\|ResourceSchema\b" internal/ --include="*.go"`
Expected: no matches (all renamed to SchemaIndex/SchemaEntry).

Run: `grep -rn "parser\.Operation\b" internal/ --include="*.go"`
Expected: no matches (all renamed to parser.Method).

Run: `grep -rn "\bOperation\b" internal/parser/ --include="*.go"`
Expected: only `OperationID` field references and `Document.Operations` slice field. No standalone `Operation` type.

- [ ] **Step 4: Commit (user-triggered)**

```bash
git add internal/codegen/gogen/gogen.go internal/codegen/gogen/gogen_test.go \
  internal/parser/project_set.go internal/parser/project_set_test.go \
  internal/parser/project_paths.go internal/parser/project_paths_test.go \
  internal/parser/model.go internal/parser/model_test.go \
  internal/parser/paths_wrapper.go internal/parser/paths_wrapper_test.go \
  internal/parser/schema_index.go internal/parser/schema_index_test.go \
  internal/parser/service.go internal/parser/service_test.go \
  internal/parser/service_name_test.go \
  internal/parser/document.go internal/parser/paths.go \
  internal/generator/ docs/superpowers/
git status
```

Wait for user to commit (do NOT auto-commit per user preference).

---

## Self-Review

**Spec coverage:**
- §1.1 gogen.Import extension → Task 1 ✓
- §1.2 PathImports → Task 2 ✓
- §1.3 Project + CreateModel/CreatePaths → Task 5 ✓
- §1.4 Model (minimal) → Task 3 ✓
- §1.4 Paths (minimal) → Task 4 ✓
- §1.5 SchemaIndex/SchemaEntry → Task 6 ✓
- §2.1 Method (rename Operation) → Task 7 ✓
- §2.2 Service + tag validation → Tasks 8, 11 ✓
- §2.3 Paths.Services/AddMethod/DeleteService → Task 9 ✓
- §2.5 Model schemasIndex/Prefix/Lookup/Index → Task 10 ✓
- §2.4 Document split (Operations []*Method) → Task 7 ✓
- Generator mechanical rename → Task 7 Step 4 ✓

**Placeholder scan:** No TBD/TODO. All code blocks complete. `serviceName` has two implementation variants (go-errors vs fmt) — picked fmt to avoid new dependency; test strings adjusted.

**Type consistency:**
- `SchemaIndex`/`SchemaEntry` used consistently in Task 6 tests + impl.
- `Method` (not `Operation`) used in Tasks 7-11.
- `Service` struct consistent across Tasks 8, 9.
- `Model.Lookup`/`Index`/`Prefix` consistent across Task 10.
- `Paths.AddMethod`/`DeleteService`/`Services` consistent across Tasks 9, 11.

**Gaps fixed:**
- Spec §1.4 had `Paths.document`/`Operations()`/`PathItems()` — simplified out (dead code in T26.1, no loader yet). Noted in plan header.
- Spec §1.3 had `Project.Document` field — not added (spec said add-then-remove; we just never add).
- `serviceName` helper — spec referenced it in §3.4 T26.2 step 6 but didn't define it in T26.1a. Added as Task 11 (it's needed for `AddMethod` callers in T26.2).

---

## Execution Handoff

Plan complete and saved to `docs/superpowers/plans/2026-07-15-t26.1-project-types-and-service-method.md`. Two execution options:

**1. Subagent-Driven (recommended)** — dispatch a fresh subagent per task, review between tasks, fast iteration.

**2. Inline Execution** — execute tasks in this session using executing-plans, batch execution with checkpoints.

Which approach?
